package com.arkhive.components.uploadmanager.uploaditem;

/**
 * Active status of UploadItem (paused, cancelled, resumed).
 * @author Chris Najar
 *
 */
public enum UploadStatus {
  PAUSED, CANCELLED, READY,
}
